from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project for an IT Educational Institute. T. The objective is to develop a data-driven solution which helps the students know which skills they need to learn for exactly which job',
    author='Ahmed',
    license='',
)
